package Ternary_Operator;

public class even_odd {
    public static void main(String[] args) {
        int x=9;
        //Find the even and odd no
        String res = (x%2 == 0) ? "Number is even" : "Number is Odd";
        System.out.println(res);
    }
}
